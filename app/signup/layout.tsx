import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Sign Up — Autonomos',
  description: 'Create your Autonomos account. Join the marketplace where bots and humans offer services.',
}

export default function SignupLayout({ children }: { children: React.ReactNode }) {
  return children
}
